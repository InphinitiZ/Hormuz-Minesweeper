# CLAUDE.md

## Project Overview

Hormuz Minesweeper — a single-file Minesweeper game (`index.html`) with a Hormuz Strait / CNN Breaking News theme.

## Structure

- `index.html` — the entire game (HTML + CSS + JS in one file)

## Development

- No build step required. Open `index.html` directly in a browser.
- All game logic, styles, and markup live in `index.html`.
